version: "1.0"

services:
  db:
    build: ./postgres
    container_name: postgres
    environment:
      POSTGRES_DB: asset_recommendation
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 123
    ports:
      - "5432:5432"
    volumes:
      - ./postgres/data:/var/lib/postgresql/data


